from math import floor

# CELL_SPACING = 0.3
# VIEW_DISTANCE = 1.5

# def cell_xyz(x, y, z, spacing=CELL_SPACING):
#     return floor(x/spacing), floor(y/spacing), floor(z/spacing)
#
#
# x, y, z = 0, 0, 0
#
#
# low_x, low_y, low_z = cell_xyz(x-VIEW_DISTANCE, y-VIEW_DISTANCE, z-VIEW_DISTANCE);
# high_x, high_y, high_z = cell_xyz(x+VIEW_DISTANCE, y+VIEW_DISTANCE, z+VIEW_DISTANCE);
#
# cell_count = 0
#
# for x in range(low_x, high_x):
#     for y in range(low_y, high_y):
#         for z in range(low_z, high_z):
#             print(x, y, z)
#             cell_count += 1

# print(f"cell_count: {cell_count}")
